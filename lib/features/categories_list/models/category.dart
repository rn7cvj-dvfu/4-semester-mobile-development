class CategoryViewModel {
  final String id;
  final String name;
  final DateTime createDate;

  const CategoryViewModel({
    required this.id,
    required this.name,
    required this.createDate,
  });
}
